"""
Zombee Product Manager Desktop.

__version__ is the single source of truth for the app's version number --
shown in the window title and the Help > About dialog, so it's always
obvious (without needing to check GitHub Actions logs or file timestamps)
exactly which build is actually running. Bump this alongside README.md's
version line whenever a new build is packaged.
"""

__version__ = "0.5.1"
